name: auth7-dev

services:
  app:
    build: .
    ports: ["8081:8081"]
    env_file:
      - path: .env
        required: true
    networks:
      - core7-infra

networks:
  core7-infra:
    external: true
    name: core7-devroot_default
