# Casbin RBAC model for Auth7

[definition]
# Sub, Obj, Act = user, resource, action
# Auth7 uses org-based isolation + branch-scoped roles

[policy_effect]
effect = allow

[matchers]
m = g(r.sub, p.sub) && regex_match(r.obj, p.obj) && regex_match(r.act, p.act)
